node_modules/
.env
.DS_Store

# Internal documents — PRDs, task splits, explainers (kept local, not published)
docs/

# Vault secrets — NEVER commit these
vault/**/*.gpg
vault/vault.key
vault/.gpg-id
